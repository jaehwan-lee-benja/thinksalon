(function() {
    const testTitle = document.getElementById("testTitle");
    testTitle.innerHTML = "hello";
})();

function countRow(layerHere) {
	const idArray = Object.keys(objectById);
	console.log("idArray = ", idArray);
}; // 테스트 js 파일이 하나 필요하다.